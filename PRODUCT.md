# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**1차 (현재):** 본인 — Claude Code와 Cursor를 함께 쓰며 바이브 코딩 자산을 지속적으로 수집·평가·실행하려는 1인 개발자.

**2차 (공개 전환 후):** Claude Code/Cursor를 쓰는 글로벌(영어권 중심) 개발자·인디해커. 이미 두 도구를 쓰고 있어 "왜 필요한가"보다 "기존 디렉터리보다 나은가"를 판단하는 단계.

## Product Purpose

VAAL(바이브코딩 자산 아카이버 & 런처)은 GitHub·awesome 리스트·공식 채널에서 Claude Code 스킬/커맨드/플러그인과 Cursor 룰을 자동 수집·아카이빙하고, 클릭 한 번으로 실행 가능한 명령어(터미널/Claude Code 버전)로 변환해주는 아카이버 겸 런처다. 평점·후기·유행도(트렌드 스코어)를 자산 데이터에 내재화해 아카이브가 커져도 우선순위 판단이 자동화되게 한다.

## Positioning

cursor.directory(월 25만 방문, 성숙한 디렉터리), awesomeclaude.ai(169개+ 카탈로그) 같은 기존 서비스는 자산을 "보여주기"까지만 한다. VAAL은 그 자산을 클릭 한 번으로 실행 가능한 터미널/Claude Code 커맨드로 즉시 변환해준다는 점에서 이들이 갖추지 못한 메커니즘을 가진다. 이 "즉시 실행 변환"이 VAAL이 정면승부를 피하고 살아남을 수 있는 유일하게 뾰족한 차별점이다.

## Operating Context

- 로컬 개인용 데스크톱 도구(Flask + SQLite, 포트 5240)로 시작해 구글 색인 + 애드센스 수익화를 목표로 한 공개 웹사이트로 전환하는 중.
- `VAAL_READONLY=1`로 공개 배포 시 쓰기 엔드포인트를 잠그는 구조가 이미 구현되어 있음(별점/설정/파이프라인 실행 등).
- 예산 $0 — 오운드/언드 채널(Reddit, HN, X, Product Hunt, Indie Hackers)만으로 초기 트래픽 확보.
- EU 방문자를 포함한 글로벌 오디언스 — 쿠키 동의(CMP)와 GDPR 대응이 향후 필요.
- 주간 자동화 파이프라인(작업 스케줄러)으로 매주 수집·아카이빙·트렌드 갱신이 이미 돌고 있음.

## Capabilities and Constraints

- **AI 비용 0원**: API 키 대신 로컬 `claude -p`(구독 CLI)를 서브프로세스로 호출. claude CLI가 없으면 규칙 기반 템플릿만으로 동작.
- **GITHUB_TOKEN 선택**: 없으면 무인증(분당 10회)으로 동작, 있으면 코드 검색까지 활성화.
- **원본 보관**: 링크만 저장하는 ClaudeRadar와 달리 SKILL.md 등 원본 파일을 로컬에 내려받아 출처가 사라져도 자산이 남음.
- **라이선스 게이팅(2026-07-29 구현)**: 공개 배포(READONLY) 모드에서는 라이선스가 허용 목록(MIT/Apache-2.0 등)에 있을 때만 원문 스니펫(600자)을 노출하고, 나머지는 출처 링크만 제공. 현재 258건 중 206건이 라이선스 미확인 상태.
- **실데이터 보유**: 258건의 실제 수집 자산(스킬 89, 플러그인 31, 룰 19, 커맨드 11, 기타 108)과 트렌드/평점 데이터가 이미 존재 — 목업이 아니라 실제 아카이브.
- **미정**: 도메인, 호스팅 방식(현재 127.0.0.1 로컬 바인딩만 되어 있어 실제 공개 배포 인프라는 아직 없음), 라이트 테마 지원 여부.

## Brand Commitments

- 제품명 "VAAL" — 헤더에 "VAAL 런처"로 표기, 별도 로고 없음.
- 기존 UI는 다크 테마 개발자 도구 톤(GitHub 다크 테마 계열 팔레트: `--bg:#0d1117`, `--accent:#58a6ff` 등)으로 이미 자리 잡음. 이 톤은 유지 대상으로 명시적으로 지정됨(리브랜드 요청 아님).

## Evidence on Hand

- 실행 중인 Flask 대시보드(`app.py`, `templates/index.html`)와 실제 258건 자산 데이터(`vaal.db`).
- 원문 아카이브 폴더(`vaa-archive/`)에 실제 다운로드된 SKILL.md/커맨드/룰 원본 다수.
- 과거 세션에서 작성된 VAAL v0.6 기획서(경쟁 리서치 16장, 수익화 재검토 17장, 저작권 체크리스트 18장 포함) — 이번 대화의 용량 계획·캠페인 계획에서 근거로 이미 사용함.
- 테스트먼트/고객 사례/가격 등은 없음 — 만들어내지 말 것.

## Product Principles

1. 발견에서 끝나지 않고 실행까지 — 저장된 자산은 항상 클릭 한 번으로 실행 가능한 형태로 끝나야 한다.
2. 로컬 우선, 원문 보관 — 출처가 사라져도 자산과 신뢰성은 남아야 한다.
3. 평가·트렌드는 부가 기능이 아니라 데이터 모델의 기본값이다.
4. 라이선스·저작권 준수는 UI 문구가 아니라 구조(게이팅 로직)로 강제한다.
5. 공개 확장은 기존 개인 도구의 정체성(다크·개발자 도구 톤)을 대체하지 않고 그 위에 신뢰를 더하는 방향으로 이루어진다.

## Accessibility & Inclusion

특정 접근성 표준이 명시적으로 요구된 적은 없음. 2026-07-29 폴리시 작업에서 카드 키보드 접근(`role=button`+`tabindex`+Enter/Space), 포커스 링을 추가함 — 최소 기준으로 유지.
